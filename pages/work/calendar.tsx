import {Redirect} from '../../components/Redirect';

const RedirectPage = () => <Redirect to='https://calendar.google.com/calendar/u/0/embed?src=adam@bluedotimpact.org&mode=WEEK'/>;

export default RedirectPage;
